# Modelos 3D (opcional)

Esta carpeta no se utiliza en la versión actual de la landing. La sección Producto usa la imagen principal (`/images/assets/Bellini_hornoprincipal.png`).

Si en el futuro se incorpora un modelo 3D (GLB), puede colocarse aquí y referenciarse desde el código.
